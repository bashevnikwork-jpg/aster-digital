# Медіа: що стоїть і що додати

## Уже в проєкті

- `public/assets/motion/logo-reveal.mp4` — анімація логотипа, у секції `#motion`.
- `public/assets/motion/ashad.mp4` — запис сайту Ashad, у картці портфоліо.

Обидва файли H.264/AAC. У браузерах відтворюються, але **перевірте у себе** —
у середовищі, де збирався сайт, немає декодера H.264, тож автоперевірку зробити
не вдалося.

## Що варто додати

1. **Постери.** До першого кадру видно чорний прямокутник. Додайте
   `poster="…"` до `<video>` у `parts.py` (секція motion і `work_card`).

2. **WebM** — менша вага й ширша підтримка:
   ```
   ffmpeg -i logo-reveal.mp4 -c:v libvpx-vp9 -crf 34 -b:v 0 -an logo-reveal.webm
   ```
   Далі `src` замінюється на два `<source>`: спершу WebM, потім MP4.

3. **Скриншот «без motion».** У секції порівняння зараз макет, зібраний кодом.
   Якщо потрібен реальний приклад статичного сайту — надішліть файл, покладемо
   в `public/assets/motion/` і поставимо поруч із відео.

4. **Вертикальні ролики** для Reels: 9:16, до 6 секунд, без звуку, 2–3 МБ.

Після будь-якої зміни: `python3 parts.py && python3 build.py`.
